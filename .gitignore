# Python
__pycache__/
*.pyc
.venv/

# Build artifacts (desktop only — not used in web version)
bin/
dist/
build/
+downloads.zip
*.zip
.build-venv/
icon.iconset/
icon.icns
build.sh
downloads.spec

# Runtime data (local only)
history.json
jobs.json
job-logs/

# OS / editor
.DS_Store
.claude/

# Secrets
.env
